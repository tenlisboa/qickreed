# .specs Project State

## Decisions

| ID | Decision | Status | Date | Context |
| -- | -------- | ------ | ---- | ------- |
| AD-001 | **Server Actions return `ActionResult<T>` (`{ data, error }` discriminated union) via `ok()`/`fail()` helpers in `src/utils/actions/types.ts`.** This is the project standard for all Server Actions going forward; migrate existing actions incrementally as touched. | active | 2026-07-09 | error-logging feature design — establishes the convention for surfacing real errors instead of swallowing them. |
| AD-002 | **No test framework.** The per-task gate is `pnpm lint && pnpm build`; final verification is behavioral (drive the app). Do not invent test commands. Overrides the spec-driven skill's test-per-AC default. | active | 2026-07-09 | CLAUDE.md states no test framework exists; user confirmed build+lint gate + behavioral verify. |
| AD-003 | **Logging: Pino** (singleton in `src/utils/logging/logger.ts`, pretty-dev/JSON-prod, secret redaction). **Error monitoring: GlitchTip via `@sentry/nextjs`** (Sentry-protocol-compatible, self-hosted; no-op when `SENTRY_DSN` unset). | active | 2026-07-09 | User chose OSS/self-hosted only; research confirmed GlitchTip as de-facto Sentry drop-in. |
| AD-004 | **Log messages in English** (machine/dev-facing); **user-facing `ActionError.message` in pt-BR** (matches existing app strings). | active | 2026-07-09 | Consistency: logs for devs, UI for pt-BR users. |
| AD-005 | **Design system = neobrutalism.** Components via `neobrutal-ui` CLI (copied into `src/components/ui/*`) + a `globals.css` token system (`:root` vars + `@theme inline`); palette is monochrome black/white/gray + **two accents** `#FFD23F` (yellow, primary/highlights/active) and `#FF6B6B` (red, error states; success is non-color); bold-everywhere (including reading screens). Supersedes the prior CLAUDE.md / `agent_docs/ui-ux_guidelines.mdc` DaisyUI + strict-monochrome mandates (rewritten by the neobrutalism-migration feature, P1-F). | active | 2026-07-09 | neobrutalism-migration feature — locked design decision; project standard for all future UI work. |

## Handoff

**Status:** neobrutalism-migration — **complete ✅ (Verifier PASS + gaps fixed)**. All 21 tasks (T1–T21) executed via per-phase sub-agents (6 phases, 8 worker dispatches + 1 Verifier); 23 commits `24eee0a`..`6953a95` on `main`. Verifier PASS: 18/20 ACs fully matched, 2 minor spec-precision gaps → **both fixed** in commit `6953a95` (`fix(ui): Button outline shadow/physics + ≥44px tap targets`): (1) NEO-04 — outline variant now has `shadow-brutal` + hover/active physics matching default/neutral; (2) NEO-18 — all Button sizes ≥44px (h-11 / icon h-11 w-11). Gate green after fix (`pnpm lint` 0 errors / 19 baseline warnings; `pnpm build` pass); zero DaisyUI repo-wide; tokens + 2-accent palette (`#FFD23F` + `#FF6B6B`) + bold-everywhere in place; docs rewritten; AD-003 Sentry wiring byte-for-byte unchanged. `validation.md` written. error-logging feature also **complete** ✅ (commits `6caa170`..`511f945`; Verifier PASS 16/16).
**Deferred to user (need browser + Supabase env + auth — not blockers):** (a) authenticated behavioral drive-through (login → dashboard → training/RSVP → assessment → admin) + browser tab-through/mobile-resize (NEO-16/18 live) — unauthenticated drive + `pnpm dev` boot confirmed clean by Verifier; (b) error-logging items: live `requestId` Pino↔`x-request-id` round-trip, `error.tsx`/`global-error.tsx` synthetic throw, real GlitchTip capture with a live DSN.
**Accepted exception:** NEO-20 — 6 recharts SVG `stroke`/`fill` hex literals in `dashboard/page.tsx` (recharts can't resolve CSS `var()` in SVG presentation attributes); recharts Tooltip itself uses tokens. Dark mode otherwise achievable via `:root` swap.
**Lesson:** L-001 (candidate) recorded via `scripts/lessons.py` — per-variant assertion discipline.
**Uncommitted (intentional baseline):** `.specs/features/neobrutalism-migration/{spec,context,design,tasks,validation}.md` + `.specs/STATE.md` — planning/validation artifacts; user chose to leave uncommitted. All implementation commits are on `main`.
**Env vars to set when plugging in GlitchTip later:** `SENTRY_DSN` (enables capture); optional `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, `SENTRY_PROJECT`, `GLITCHTIP_URL` (sourcemap upload). All unset → app runs fully functional, no-op.
**Files of record:** `.specs/features/error-logging/{spec,design,tasks,validation}.md`; `.specs/features/neobrutalism-migration/{spec,context,design,tasks,validation}.md`